export type SetCollectionList = {
    _id: string;
    card_name: string;
    image_url: string;
    desc: string;
}

export type SetCollectionGallery = {
    _id: string;
    card_name: string;
    image_url: string;
}